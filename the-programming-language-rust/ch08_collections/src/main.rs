use ::collections::hashmap;
use ::collections::string;
use ::collections::vector;

fn main() {
    vector::test_push_with_reference();
    vector::iterate_vector();
    vector::vector_with_enum();

    string::slice_string();

    hashmap::update_hashmap();
}
